pipeline {
    agent any 

    stages{

        stage("build"){
            steps{
                echo "Jenkins in build Var $BRANCH_NAME"
            }
        }

        stage("test"){
            steps{
                echo "Ready for test "
            }
        }

    }
}